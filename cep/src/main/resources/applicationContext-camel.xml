<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:camel="http://camel.apache.org/schema/spring"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
       http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd">
	
	<bean id="msg.processor" 
		  class="com.octo.monitoring_flux.cep.processor.MonitoringMessageProcessor" />
	
	<!-- ****************** -->
	<!-- *  Camel Context * -->
	<!-- ****************** -->
	<camelContext  xmlns="http://camel.apache.org/schema/spring" trace="false">
		
		<!-- FrontEnd .to. CEP .to. ElasticSearch -->
		<route id="audit.frontend" trace="false">
			<from uri="jeromq://tcp://127.0.0.1:${zeromq.frontend.port}?socketType=PULL&amp;linger=0&amp;label=frontend" />
			<to uri="msg.processor" />
			<multicast stopOnException="true">
				<to uri="esper://monitoring" />
				<to uri="elasticsearch://${elasticsearch.cluster.name}?operation=INDEX&amp;indexName=${elasticsearch.index.name}&amp;indexType=cep_to_elasticsearch"/>
			</multicast>
		</route>

		<route id="middleend" trace="false">
			<from uri="jeromq://tcp://127.0.0.1:${zeromq.middleend.port}?socketType=PULL&amp;linger=0&amp;label=middleend" />
			<to uri="msg.processor" />
			<multicast stopOnException="true">
				<to uri="esper://monitoring" />
				<to uri="elasticsearch://${elasticsearch.cluster.name}?operation=INDEX&amp;indexName=${elasticsearch.index.name}&amp;indexType=cep_to_elasticsearch"/>
			</multicast>
		</route>

		<route id="backend" trace="false">
			<from uri="jeromq://tcp://127.0.0.1:${zeromq.backend.port}?socketType=PULL&amp;linger=0&amp;label=backend" />
			<to uri="msg.processor" />
			<multicast stopOnException="true">
				<to uri="esper://monitoring" />
				<to uri="elasticsearch://${elasticsearch.cluster.name}?operation=INDEX&amp;indexName=${elasticsearch.index.name}&amp;indexType=cep_to_elasticsearch"/>
			</multicast>
		</route>

		<!-- Processing CEP and COUNT -->
		<route id="cep.count_per_second" trace="false">
			<from uri="esper://monitoring?eql=insert into TicksPerSecond
						select correlationId, count(*) as cnt
						from com.octo.monitoring_flux.shared.MonitoringEvent.win:time_batch(1 sec)
						group by correlationId" />
			<to uri="esper://monitoring" />
		</route>

		<!-- Detect violation and push to ES -->
		<route id="cep.violation" trace="false">
			<from uri="esper://monitoring?eql=
						select correlationId, avg(cnt) as avgCnt, cnt as MsgCnt
						from TicksPerSecond.win:time(10 sec)
						group by correlationId" />
			<to uri="elasticsearch://${elasticsearch.cluster.name}?operation=INDEX&amp;indexName=${elasticsearch.index.name}&amp;indexType=violation " />
		</route>
            			
	</camelContext>
	
</beans>